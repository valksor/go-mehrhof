{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q 'git stash' && echo '{\"decision\":\"block\",\"reason\":\"BLOCKED: git stash is forbidden in this project. Do not stash — commit your work or use a branch instead.\"}' || true"
          }
        ]
      }
    ]
  }
}
